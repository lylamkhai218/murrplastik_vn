<IfModule mod_rewrite.c>
  RewriteEngine On

  # 1. Tự động chuyển hướng từ HTTP sang HTTPS bảo mật
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # 2. Tự động chuyển hướng từ WWW sang Non-WWW (Ví dụ: www.murrplastikvn.com -> murrplastikvn.com)
  RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
  RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
</IfModule>

# 3. Nén GZIP (Giảm dung lượng file khi tải qua mạng)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json image/svg+xml
</IfModule>

# 4. Cấu hình Browser Caching (Lưu bộ nhớ đệm trình duyệt)
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresDefault "access plus 1 month"
  
  # CSS và JS
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType text/javascript "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  
  # Hình ảnh & Media
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/x-icon "access plus 1 year"
</IfModule>

# 5. Thiết lập mã hóa UTF-8 mặc định cho tất cả các file
AddDefaultCharset UTF-8
AddCharset UTF-8 .html .css .js .xml .json
